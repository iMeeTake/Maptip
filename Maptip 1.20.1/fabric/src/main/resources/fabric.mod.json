{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "modmenu.descriptionTranslation.${mod_id}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "homepage": "${homepage}",
    "sources": "${sources}",
    "issues": "${issues}"
  },
  "license": "${license}",
  "icon": "${mod_id}-icon.png",
  "environment": "client",
  "entrypoints": {
    "main": [
      "${group}.${mod_id}.fabric.Fabric${mod_main}"
    ],
    "client": [
      "${group}.${mod_id}.fabric.client.Fabric${mod_main}Client"
    ],
    "modmenu": [
      "${group}.${mod_id}.fabric.client.Fabric${mod_main}ModMenu"
    ]
  },
  "mixins": [
    "${mod_id}.fabric.mixins.json"
  ],
  "depends": {
    "java": ">=${java_version}",
    "minecraft": "${minecraft_version}",
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_version}",
    "balm-fabric": ">=${minimum_balm_version}"
  },
  "custom": {
    "modmenu": {
      "links": {
        "modmenu.modrinth": "https://modrinth.com/mod/maptip"
      }
    }
  }
}
